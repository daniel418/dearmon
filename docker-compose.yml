services:
  dearmon:
    build: .
    image: dearmon:latest
    container_name: dearmon
    restart: unless-stopped
    # 只開到 127.0.0.1,讓本機 nginx 反向代理,不直接對外
    # host:3001 → container:3000(避開已佔用 3000 的 Grafana)
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      NODE_ENV: production
